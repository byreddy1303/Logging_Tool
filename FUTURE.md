# Future / deferred

Ideas that came up during build but are **not** to be built in v1.0.0. Do not add to `BUILD.md`. Do not implement. This file is a graveyard, not a backlog.

Format:

```
- <one-line idea> — <why deferred>
```

If a truly critical feature is missing, propose it to the user in text — do not silently implement.

---

- Live chat between buddies — explicit ban (see BUILD.md §17)
- LLM auto-tagging of outcome / root cause — explicit ban
- Score leaderboards — ego trap, banned
- Public sign-up — invite-only enforced
- Mobile-native apps — PWA is enough for two users
- Real-time question co-solving — sync distraction
- Notifications of any kind — explicit ban
- AI-generated study plan — the plan is yours; tool doesn't override
