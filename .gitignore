node_modules
dist
dist-ssr
.env
.env.local
.env.production
*.local
.DS_Store
.vscode
.idea
supabase/.branches
supabase/.temp
supabase/functions/**/.env
coverage
playwright-report
test-results

# Native build outputs and machine-local Android configuration
android/.gradle
android/.idea
android/local.properties
android/app/build
android/build
*.jks
*.keystore
keystore.properties

# Build artefacts that leak from tsc / vite type-checking
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Deploy secrets — sourced by scripts/deploy.sh, never committed
.deploy.env
.vercel
.env*
# ...but keep the templates so `git clone` gives new operators a starting shape
!.env.example
!.deploy.env.example
!.env.capacitor.example
