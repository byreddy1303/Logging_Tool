# Copy to android/keystore.properties and fill locally. Never commit the real file.
# storeFile is resolved relative to android/.
storeFile=../../release/air-journal-upload.jks
storePassword=
keyAlias=air-journal-upload
keyPassword=
